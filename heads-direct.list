AND,((PROTOCOL,UDP),(DST-PORT,443),(GEOIP,CN,no-resolve))
AND,((PROTOCOL,UDP),(DST-PORT,3478),(GEOIP,CN,no-resolve))
AND,((PROTOCOL,UDP),(DST-PORT,3549),(GEOIP,CN,no-resolve))
AND,((PROTOCOL,UDP),(DST-PORT,19302),(GEOIP,CN,no-resolve))
AND,((PROTOCOL,UDP),(DST-PORT,19305),(GEOIP,CN,no-resolve))
AND,((PROTOCOL,UDP),(DST-PORT,853),(GEOIP,CN,no-resolve))
